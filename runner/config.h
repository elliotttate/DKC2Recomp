#pragma once

/* The shared headless runtime includes the game config header for historical
 * compatibility. DKC2's first integration probe has no user-facing options;
 * the future desktop runner will define its own explicit configuration. */
